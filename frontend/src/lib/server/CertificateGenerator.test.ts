import {describe, expect, it} from 'vitest';

import {generateCertificateFor} from "$lib/server/CertificateGenerator";

describe('load an image', () => {
    it('loads the correct file by checking its size', async () => {
        await generateCertificateFor("nylCqCwRnhr26khFEUw8oj")
    });
});
